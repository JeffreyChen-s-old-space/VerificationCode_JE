version: 2.1

orbs:
  maven: circleci/maven@1.0.3

workflows:
  maven_test:
    jobs:
      - checkout
      - run:
          name: Install Font
          command: apt-get install -y libfontconfig1
      - maven/test:
          test_results_path: /test/java/Module